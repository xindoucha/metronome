# 节拍器 Metronome — 设计文档

## 设计理念

应用分为两个核心 tab：**节拍器** 和 **练习谱**。节拍器页只暴露最高频操作：BPM 调节、细分拍、音量、播放控制。拍号、音色、节拍强弱等配置性功能收入底部抽屉，保持界面简洁的同时不损失专业能力。练习谱页提供适合初学者的架子鼓基本功路径，可一键套用到节拍器设置。

---

## 技术实现

### 音频精度：Lookahead Scheduler

采用**前瞻调度**方案避免 JS 线程阻塞导致的节拍漂移：

- 每 25ms 触发一次调度器，提前 120ms 向 Web Audio API 写入音频事件
- 真实鼓组类音色使用本地 one-shot 鼓采样，开始播放前完成加载和解码
- 合成音色继续通过 Web Audio API 实时生成，作为轻量音色和采样失败时的 fallback

### 音色系统

| 音色 | 实现方式 |
|------|----------|
| 真实鼓组 | CC0 底鼓 / 军鼓 / 踩镲采样分层 |
| 原声节拍 | rim / 底鼓 / 踩镲采样组合，更接近练习器点击感 |
| 踩镲计拍 | closed hat 采样作为主计拍音 |
| 经典 | Square 振荡器（高频瞬态）+ Sine（低频共鸣） |
| 计时器 | 高频方波短包络 + 带通噪声瞬态 + 重音低频 tock |
| 合成鼓组 | 底鼓扫频、军鼓噪声、踩镲高通噪声组合 |
| 木块 | 带通滤波白噪声 + Sine 基频 |
| 铃声 | 双 Sine 叠加（基频 + 2.756× 泛音），长衰减 |
| 电子 | Sine 方波包络，线性淡入淡出 |
| 拍手 | 三层时移白噪声，模拟手掌拍击声纹 |
| 踢鼓 | Sine 频率扫描（190Hz → 38Hz），配合方波瞬态 |
| 口哨 | Sine 音高滑动（上升-峰值-轻微回落） |
| 三角铁 | 高频 Sine（3200Hz）+ 1.48× 泛音，超长衰减 |
| 钢琴 | 5 次谐波叠加 + 方波锤击瞬态 |

---

### 练习谱渲染

架子鼓练习谱由 `js/practice.js` 单独维护：

- 基础知识模块用图文卡片解释音符、鼓谱位置、击打类型、读谱顺序和达标标准
- 练习数据用数组描述踩镲、军鼓、底鼓的落点
- 哑鼓垫练习用单线军鼓谱和 R / L 手序标记描述左右手
- 15 天计划模块用任务数据描述阶段、每日目标、练习时长、建议 BPM、练习要点和达标标准
- 使用本地 vendor 的 VexFlow 4.2.5 渲染五线谱、打击乐谱号、4/4、时值排版、符干和横梁
- 对 VexFlow 输出做轻量后处理：踩镲使用 `x` 符头，军鼓 / 底鼓使用圆符头
- 谱面区域在移动端内部横向滚动，页面本体不横向溢出

---

## 主要功能

### BPM 控制
- 范围 20 ~ 240 BPM，自动显示对应速度术语（Larghissimo → Prestissimo）
- 点击数字直接键盘输入；拖动滑块连续调节；`−` / `+` 按钮单步调节（长按快速步进）
- **Tap Tempo**：连续点击取最近 6 次间隔均值

### 细分拍
在每个主拍之间插入等分子拍，共 4 种，以音符记谱图标区分：

| 选项 | 说明 |
|------|------|
| 四分 ♩ | 仅主拍 |
| 八分 ♩♩ | 主拍 + 1 个子拍，单横梁 |
| 三连 ♩♩♩ | 主拍 + 2 个子拍，三连音 |
| 十六分 ♬♬ | 主拍 + 3 个子拍，双横梁 |

### 拍号（底部抽屉）
- 独立调节每小节拍数（1 ~ 16）和音符时值（1/2、1/4、1/8、1/16）
- 8 种快速预设：2/4、3/4、4/4、6/8、5/4、7/8、9/8、12/8

### 音色（底部抽屉）
- 14 种音色，点击即切换并试听预览
- 默认使用「真实鼓组」，新增「原声节拍」「踩镲计拍」满足练鼓场景
- 保留「计时器」和「合成鼓组」等 Web Audio 合成音色，网络或采样加载失败时仍可使用

### 节拍强弱（底部抽屉）
每一拍可独立设置四级力度：**重音**（琥珀色）、**普通**（紫色）、**弱拍**（灰色）、**静音**，主界面入口以彩色小点实时预览。

### 其他
- **音量滑块**：全局音量调节
- **双色主题**：浅色 / 深色，顶部拨杆切换，自动跟随系统偏好
- **键盘快捷键**：`Space` 播放/停止，`↑` / `↓` BPM ±1，`T` Tap Tempo，`Esc` 关闭抽屉

### 架子鼓基本功练习
- 默认进入「基础知识」，帮助新手先理解音符、鼓谱位置和击打类型
- 练习顺序从哑鼓垫开始，再进入整套鼓节奏
- 练习页使用模块选择器：基础知识 / 哑鼓垫 / 架子鼓 / 15天计划；下方只展示当前知识、谱面或任务，减少滚动成本
- 5 个基础知识主题：音符时值、鼓谱位置、击打类型、读谱顺序、练习达标
- 9 条哑鼓垫左右手练习：八分单击、十六分单击、双击、单双混合、Paradiddle、重音移动和稳定耐力
- 7 条整套鼓练习：四分稳拍、八分律动、十六分控制、四分到八分切换、八分到十六分切换、综合循环、《鼓楼》入门伴奏型
- 15 天计划按 3 个阶段安排每日目标、练习时长、建议 BPM、练习要点和达标标准
- 每条包含练习目标、建议 BPM、标准鼓谱视觉和「套用」按钮
- 「套用」会切回节拍器页，并设置对应 BPM 与细分拍

---

## 兼容性

纯原生 HTML / CSS / JS，无第三方依赖，静态文件部署。支持 PC 端与移动端（iOS Safari / Android Chrome）响应式适配。
